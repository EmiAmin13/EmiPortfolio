# URL del webhook de n8n para el asistente.
# Local (Docker):  http://localhost:5678/webhook/asistente
# Si no está definida, el chat usa respuestas locales (modo demo).
# Copiá este archivo a .env.local y descomentá:
# VITE_N8N_WEBHOOK_URL=http://localhost:5678/webhook/asistente
